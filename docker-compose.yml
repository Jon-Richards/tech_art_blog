version: "3.9"

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"

  node:
    image: node:14.15-alpine
    working_dir: /app
    volumes:
      - .:/app
